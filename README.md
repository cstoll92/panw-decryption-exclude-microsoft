# panw-decryption-exclude-microsoft
cript that pulls down the current set of domains published by microsoft that require SSL decryption and automatically adds these domains to the Prisma Access global ssl decryption exclusions.
